<h1>Edinng Book</h1>
<%= form_for(@book) do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>


    <h4>Body</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Update Book' %>
    <% if flash[:notice] %>
   <div class="flash">
   <%= flash[:notice] %>
      </div>

  <% end %>


<% end %>

<%= link_to "Back", books_path %>
<%= link_to "show", book_path %>